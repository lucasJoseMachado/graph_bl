@graph_bl = angular.module('graph_bl', ['ngRoute', 'ngAnimate', 'ngResource', 'templates'])

.config(['$routeProvider', ($routeProvider) ->
  $routeProvider
    .otherwise({templateUrl: "main.html", controller: 'MainCtrl'})
])
